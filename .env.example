# 🔧 Variables de Entorno - Ejemplo

# Este archivo sirve como plantilla para las variables de entorno del proyecto.
# Copia este archivo a `.env.local` y rellena los valores necesarios.

# ==========================================
# CONFIGURACIÓN DE DESARROLLO
# ==========================================

# Entorno de ejecución (development, production, test)
NODE_ENV=development

# ==========================================
# API KEYS (si los endpoints las requieren)
# ==========================================

# Ejemplo: Si usaras una API que requiere autenticación
# API_KEY=tu_clave_aqui
# API_SECRET=tu_secreto_aqui

# ==========================================
# URLs DE APIS EXTERNAS (opcional)
# ==========================================

# Puedes configurar las URLs base de las APIs para fácil cambio
# ADVICE_API_URL=https://api.adviceslip.com
# DOG_API_URL=https://dog.ceo/api

# ==========================================
# CONFIGURACIÓN DE SEGURIDAD (opcional)
# ==========================================

# CORS: Dominios permitidos
# ALLOWED_ORIGINS=http://localhost:3000,https://tu-dominio.com

# Rate limiting (opcional, si implementas rate limiting)
# RATE_LIMIT_MAX_REQUESTS=100
# RATE_LIMIT_WINDOW_MS=60000

# ==========================================
# NOTAS IMPORTANTES
# ==========================================

# 1. NUNCA commitees el archivo .env.local al repositorio
# 2. Añade .env.local al .gitignore
# 3. Para producción, configura las variables en tu plataforma de deployment
# 4. Las variables de entorno son accesibles solo en el servidor (Route Handlers)
# 5. Para variables del cliente, usa el prefijo NEXT_PUBLIC_

# Ejemplo de variable pública (accesible en el navegador):
# NEXT_PUBLIC_API_URL=https://api.example.com

